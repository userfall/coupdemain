export function DemoModeBadge() {
  return (
    <span className="inline-flex items-center rounded-full bg-brand-soft px-4 py-2 text-sm font-semibold text-brand-strong">
      Base locale active
    </span>
  );
}
